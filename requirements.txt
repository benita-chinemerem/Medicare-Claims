# ============================================================
# fraud-anomaly-ai — Python Dependencies
# Updated for Python 3.13 compatibility
# ============================================================

# --- Core data stack ----------------------------------------
pandas==2.2.3             # 2.2.x is the first pandas series with Python 3.13 support
numpy==2.1.3              # 2.x required for Python 3.13
pyarrow==18.1.0           # Parquet read/write

# --- Database -----------------------------------------------
sqlalchemy==2.0.36
psycopg2-binary==2.9.10   # PostgreSQL adapter

# --- ML models ----------------------------------------------
scikit-learn==1.5.2       # Isolation Forest, StandardScaler, metrics
xgboost==2.1.3            # Supervised demonstration layer
shap==0.46.0              # SHAP explainability

# --- Testing ------------------------------------------------
pytest==8.3.4
pytest-cov==6.0.0

# --- Utilities ----------------------------------------------
python-dotenv==1.0.1      # Load .env for local script runs
tqdm==4.67.1              # Progress bars for long ETL runs